# Database (PostgreSQL — required for Vercel; use Neon, Supabase, or Vercel Postgres)
DATABASE_URL="postgresql://user:password@host:5432/deadegos?sslmode=require"

# Admin portal
ADMIN_PASSWORD="200Orders!"
SESSION_SECRET="change-this-to-a-random-secret-in-production"

# Public site URL (no trailing slash)
NEXT_PUBLIC_SITE_URL="https://deadegos.co"

# Stripe (https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# SendGrid (Twilio email) — order confirmation & shipped emails
SENDGRID_API_KEY="SG_..."
SENDGRID_FROM_EMAIL="DeadEgos <orders@deadegos.co>"

# Twilio — shipping SMS (https://console.twilio.com)
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_PHONE_NUMBER=""

# Vercel Blob — product image uploads in production (auto-set when Blob store is linked)
# BLOB_READ_WRITE_TOKEN=""
